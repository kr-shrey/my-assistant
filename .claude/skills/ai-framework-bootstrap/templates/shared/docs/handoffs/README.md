# Handoffs

Dated session handoff notes: `docs/handoffs/YYYY-MM-DD.md`.

Produced by `/wrapup` at session end. Injected into context at the next `SessionStart`
(if hooks are configured) so work resumes cleanly.
