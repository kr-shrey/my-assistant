# Requirements

One self-contained package per domain: `docs/requirements/<domain>/` containing
`clarification.md`, `functional-reqs.md`, `nonfunctional-reqs.md`, `traceability.md`.

Produced by the **analyst** agent (`/analyze <domain>`). Read by the **architect**.
