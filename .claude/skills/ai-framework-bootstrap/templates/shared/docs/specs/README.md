# Specs

Technical specifications, one per feature/domain: `docs/specs/<feature>-spec.md`.

Produced by the **architect** agent (`/architect <feature>`) from the requirements package.
Read by the **senior-developer** to break into tasks. ADRs live in `../decisions/`.
