---
name: save-session
description: >
  Save current agent session progress to a named checkpoint under docs/sessions/<name>.md.
  Usage: /save-session <session-name> [summary]
---

# Save Session Checkpoint

Save active session state and progress summary to `docs/sessions/$ARGUMENTS.md`.

Read `.claude/skills/session-checkpoint/SKILL.md` and follow Section 3 (`/save-session`) to emit the structured session file.
